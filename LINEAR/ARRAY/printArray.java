package Array;
import java.util.Arrays; // Import the Arrays clas
public class printArray { // Class name should start with an uppercase letter
    public static void main(String[] args) {
        int[] arr = {2, 4, 6, 8, 10}; // Initialize the array
        System.out.println("Your array is: " + Arrays.toString(arr)); // Print the array using Arrays.toString()
    }
}